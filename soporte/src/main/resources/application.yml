server:
  port: 8081

spring:
  application:
    name: soporte
  datasource:
    url: jdbc:mysql://localhost:3306/nubos_soporte?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=America/Santiago
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
