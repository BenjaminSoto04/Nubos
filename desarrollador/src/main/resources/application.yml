server:
  port: 8090

spring:
  application:
    name: desarrollador
  datasource:
    url: jdbc:mysql://localhost:3306/nubos_desarrollador?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=America/Santiago
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
