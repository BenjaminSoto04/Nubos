server:
  port: 8089

spring:
  application:
    name: promocion
  datasource:
    url: jdbc:mysql://localhost:3306/nubos_promocion?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=America/Santiago
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
