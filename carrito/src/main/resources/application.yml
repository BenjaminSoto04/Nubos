server:
  port: 8088

spring:
  application:
    name: carrito
  datasource:
    url: jdbc:mysql://localhost:3306/nubos_carrito?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=America/Santiago
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
